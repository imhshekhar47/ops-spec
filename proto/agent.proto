syntax = "proto3";

option go_package="./pb";

import "common.proto";

message Agent {
    Metadata meta = 1;
    string uuid = 2;
    string address = 3; 
}

message AgentList {
    repeated Agent items = 1;
}

message AgentRequest {
    string uuid = 1;
}
